import { Button } from "@rneui/base";
import Login from "../screen/Auth";
import Home from "../screen/Home";
import Week from "../screen/Week";

export const LoginRoutes = {
  name: "Login",
  component: Login,
};

export const HomeRoutes = {
  name: "Home",
  component: Home,
};

export const WeekRoutes = {
  name: "Week",
  component: Week,
};
